name: p256_example
description: Demonstrates how to use the p256 plugin.
publish_to: 'none'

environment:
  sdk: '>=2.15.0 <3.0.0'
  flutter: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  secp256r1:
    path: ../

  agent_dart: ^1.0.0-0
  convert: any
  crypto: any
  tuple: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
